services:
  - type: web
    name: artfolio
    env: node
    rootDir: .
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
